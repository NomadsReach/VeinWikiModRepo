image: ubuntu:18.04
pages:
  stage: deploy
  script:
    - mkdir -p public
    - shopt -s extglob
    - cp -r !(public) public/
    - rm -rf public/.git
    - rm -rf public/.gitlab-ci.yml
  artifacts:
    paths:
      - public
  only:
    - main
    - master

